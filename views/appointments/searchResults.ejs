<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Search Teachers</title>
  <style>
    body { font-family: Arial, sans-serif; padding: 20px; }
    ul { list-style: none; padding: 0; }
    li { margin: 10px 0; padding: 10px; border: 1px solid #ccc; border-radius: 5px; }
    a.button { padding: 5px 10px; background-color: #007BFF; color: white; text-decoration: none; border-radius: 5px; }
  </style>
</head>
<body>
  <h2>Search Results for "<%= query %>"</h2>

  <% if (teachers.length === 0) { %>
    <p>No teachers found.</p>
  <% } else { %>
    <ul>
      <% teachers.forEach(t => { %>
        <li>
          <strong><%= t.name %></strong> — <%= t.department %><br>
          <a class="button" href="/book/<%= studentId %>?teacherId=<%= t._id %>">Book Appointment</a>
        </li>
      <% }); %>
    </ul>
  <% } %>

  <br>
  <a href="/appointments/student/<%= studentId %>">← Back to Appointments</a>
</body>
</html>
